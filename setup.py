from setuptools import setup

package = 'spec-py'
version = '0.1'

INSTALL_REQUIRES = ['toolz>=0.8']

setup(name=package,
      version=version,
      description="Simple specification",
      url='')
